/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'cf.bstatic.com', 'photos.hotelbeds.com'],
  },
}

module.exports = nextConfig
